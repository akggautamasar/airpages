"use client";import{BookOpen,ChevronLeft,ChevronRight,Focus,StickyNote,Maximize2}from"lucide-react";
export function StudyToolbar({page,count,prev,next,focus,onFocus,notes,onNotes}:{page:number;count:number;prev:()=>void;next:()=>void;focus:boolean;onFocus:()=>void;notes:boolean;onNotes:()=>void}){return <div className="study-toolbar"><button onClick={prev} disabled={page===0}><ChevronLeft size={17}/>Previous</button><div className="toolbar-center"><span className="page-pill">{page+1} / {count}</span><button className={focus?"active":""} onClick={onFocus}><Focus size={15}/>Focus</button><button className={notes?"active":""} onClick={onNotes}><StickyNote size={15}/>Notes</button><button><Maximize2 size={15}/>Full</button></div><button onClick={next} disabled={page===count-1}>Next<ChevronRight size={17}/></button></div>}
