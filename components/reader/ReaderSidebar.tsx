"use client";import type{ReaderDocument}from"@/lib/document";
export function ReaderSidebar({document,currentPage,onSelect}:{document:ReaderDocument;currentPage:number;onSelect:(n:number)=>void}){return <aside className="sidebar"><div className="eyebrow">DOCUMENT</div><div className="sidebar-title">{document.title}</div><div className="sidebar-meta">{document.author} · {document.pageCount} pages</div><div className="thumb-list">{document.pages.map(p=><button key={p.index} onClick={()=>onSelect(p.index)} className={p.index===currentPage?"thumb active":"thumb"}><div className="thumb-page"><span>{String(p.index+1).padStart(2,"0")}</span><i/><i/><i/></div><span>{p.label}</span></button>)}</div></aside>}
