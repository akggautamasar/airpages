# Three Paper

AirPages uses the supplied ThreeDPaper design as a page-presentation reference. The source describes Three.js r149, custom GLSL and CanvasTexture, with no required binary assets. The AirPages adapter keeps the document page separate from the presentation engine so any PDF/EPUB-rendered page can be reused.